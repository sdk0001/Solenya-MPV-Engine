# Native Libraries

This directory contains the native MPV stack packaged by the Solenya MPV Engine plugin.

The following ABIs are provided in this release:

- `arm64-v8a`
- `armeabi-v7a`

Build provenance and rebuild commands are documented in `../NATIVE_BUILD.md`.

Checksums are recorded in `SHA256SUMS.txt`.
